# Database
POSTGRES_DB=diagnyx
POSTGRES_USER=diagnyx
POSTGRES_PASSWORD=diagnyx123

# JWT Secrets
JWT_SECRET=dev-jwt-secret-change-in-production

# Services URLs (Simplified Architecture)
USER_SERVICE_URL=http://user-service:8080
API_GATEWAY_URL=https://diagnyx-api-gateway:8443
UI_URL=http://diagnyx-ui:3000

# External Services (Optional)
OPENAI_API_KEY=sk-proj-xxxxx
ANTHROPIC_API_KEY=sk-ant-xxxxx

# Environment
ENVIRONMENT=development
LOG_LEVEL=debug

# Monitoring
PROMETHEUS_URL=http://prometheus:9090
GRAFANA_URL=http://grafana:3000

# Note: Simplified architecture - removed:
# - REDIS_PASSWORD (Redis removed)
# - AUTH_URL (merged into user-service)
# - DASHBOARD_URL (removed)
# - Multiple LLM service URLs (services removed)